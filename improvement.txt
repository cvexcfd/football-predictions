Redesign the entire frontend with a world-class visual identity: dark theme with deep navy/charcoal base, vibrant accent colors (electric blue + gold), glassmorphism cards with subtle borders and backdrop blur, smooth CSS transitions and hover animations on all interactive elements, a bold typographic hierarchy using a modern sans-serif (e.g. Inter or Outfit via Google Fonts), football-themed background textures or subtle pitch grid pattern, team flag images displayed prominently on match cards, a sticky gradient navbar with logo, animated leaderboard rows with rank badges, badge cards with glow effects matching their tier, skeleton loading states, fully responsive mobile-first layout with bottom tab navigation on small screens, and empty states with illustrated SVG placeholders — make every screen feel like a premium sports app, not a CRUD interface.Redesign the match cards to look premium: show both team flags side by side with team codes in bold, a VS divider with the kickoff time and stage badge centered below, a pulsing green dot for live matches, color-coded result badges (exact score gold, correct result silver, wrong red), smooth reveal animation on load, and a compact prediction input with +/- stepper buttons styled like a scoreboard.Integrate high-quality real football visuals throughout the app using only free licensed sources — no AI-generated images: for the login/hero section fetch a looping stadium atmosphere video from Pexels (search "football stadium crowd night" at pexels.com/videos, pick MP4, embed as a muted autoplay loop behind a dark overlay with parallax scroll effect using CSS transform translateY tied to window.scrollY); for section backgrounds use Unsplash Source API with fixed football queries like https://source.unsplash.com/1920x1080/?football,stadium as CSS background-image with background-attachment:fixed to achieve pure CSS parallax; for team flags use flagcdn.com dynamically via https://flagcdn.com/w80/{countrycode}.png — no downloads needed; for icons (trophy, ball, shield, medal, badge, chart) use only Lucide React which is already in the stack; for illustration empty states use unpkg-hosted SVGs from undraw.co matching the dark theme color; implement scroll-triggered entrance animations using IntersectionObserver so cards, leaderboard rows and stats slide in